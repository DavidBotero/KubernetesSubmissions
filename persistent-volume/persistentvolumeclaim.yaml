apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pingpong-log-pvc
spec:
  storageClassName: pingpong-log-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
